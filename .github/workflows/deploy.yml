name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy trigger
        run: echo "Ready for deployment. Connect Railway or Render for auto-deploy."
